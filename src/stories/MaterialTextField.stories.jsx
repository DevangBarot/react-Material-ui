import React from 'react';
import { MaterialTextField } from './MaterialTextField';

export default {
  title: 'Example/MaterialTextField',
  component: MaterialTextField,
};

const Template = (args) => <MaterialTextField {...args} />;
export const DefaultMatCheckbox = Template.bind({});